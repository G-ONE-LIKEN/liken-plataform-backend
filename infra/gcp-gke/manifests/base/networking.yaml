apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: liken-frontend-backend-config
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /api/health
---
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: liken-gateway-backend-config
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /actuator/health
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: liken-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
    networking.gke.io/managed-certificates: "liken-managed-cert"
    networking.gke.io/v1beta1.FrontendConfig: "liken-frontend-config"
spec:
  rules:
    - http:
        paths:
          - path: /internal/kyc/webhook
            pathType: Prefix
            backend:
              service:
                name: user-service
                port:
                  number: 8080
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
