meta {
  name: reject-project
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/projects/1/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "reason": "Documentación incompleta."
  }
}
