info:
  name: bad-request
  type: http

http:
  method: POST
  url: http://localhost:8090/api/auth/register/request?Content-Type=application/json
  params:
    - name: Content-Type
      value: application/json
      type: query
  body:
    type: json
    data: |-
      {
        "email": "federicoclarosv.15@gmail.com",
        "password": "mipass123!"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
