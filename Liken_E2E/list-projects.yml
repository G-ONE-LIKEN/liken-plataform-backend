info:
  name: list-projects
  type: http

http:
  method: GET
  url: http://localhost:8090/api/projects
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
