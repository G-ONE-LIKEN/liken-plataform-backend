info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8090/api/auth/register/request
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "federicoclarosv.15@gmail.com",
        "password": "mipass123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
